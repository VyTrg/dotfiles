# This is zed setting

> [!WARNING]
> Because Zed only supports LSP not Offical Oracle Java, so I use `brew` to install `jdlts` (this is the easiest and fastest way)

> [!NOTE]
> `/usr/local/bin/jdtls` should be downloaded properly from eclipse jdt-ls server then unzip into above direction

